[project]
name = "obp-mcp"
version = "0.1.0"
description = "An MCP Server for the Open Bank Project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.21.2",
    "requests>=2.32.5",
    "starlette>=0.50.0",
    "uvicorn>=0.38.0",
    "watchfiles>=1.0.3",
]
